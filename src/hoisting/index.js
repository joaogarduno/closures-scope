console.log(nameOfDog);
var nameOfDog = 'Snoopy';
console.log(nameOfDog);

// --------------------------------
// Vamos a probar con una funcion

// Mandamos a llamar antes la funcion
nameOfDog();

function nameOfDog(){
    console.log(`El mejor perro es ${namePerro}`);
}

// var namePerro = 'Snoopy';

// Que pasaria si ejecutamos el siguiente codigo sin nada, es decir si le quitamos el siguiente codigo (var namePerro = 'snoopy')
// var namePerro = 'Snoopy';

// Pero si agregamos o definimos una varible si se ejecuta el codigo
var namePerro;
